import { BrMoneyFormatterPipe } from './br-money-formatter.pipe';

describe('BrMoneyFormatterPipe', () => {
  it('create an instance', () => {
    const pipe = new BrMoneyFormatterPipe();
    expect(pipe).toBeTruthy();
  });
});
