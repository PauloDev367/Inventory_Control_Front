import { MovementTypeFormatPipe } from './movement-type-format.pipe';

describe('MovementTypeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new MovementTypeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
