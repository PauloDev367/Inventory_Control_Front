import { BrTimeFormatPipe } from './br-time-format.pipe';

describe('BrTimeFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new BrTimeFormatPipe();
    expect(pipe).toBeTruthy();
  });
});
