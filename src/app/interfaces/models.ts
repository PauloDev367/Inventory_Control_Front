export interface Product {
    createdAt: string,
    deletedAt: string,
    description: string,
    id: string,
    minimumStock: number,
    name: string,
    price: number,
    quantity: number,
    supplierId: string
}